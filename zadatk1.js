let b = ["LAlala je lalall "];
let r=[]
let brojac=0;
let a = (l) => { 
    const pola= Math.ceil(l.length/2);
    const prvapol=l.slice(0,pola.length)
    const drugapol=l.slice(pola.length)
 console.log("Ispis:", l + "->" + drugapol) 
}

a(b)

